import { orOnlyOnBooleans } from './or-only-on-booleans';
import { stricterTypescript } from './stricter-typescript';

export const rules = {
  'stricter-typescript': stricterTypescript,
  'or-only-on-booleans': orOnlyOnBooleans,
};
